export default function Pagination({ pages = 1, data = [], maxPages = 1 }) {
  return (
    <div>
      <button type="">Back</button>
      <div>{}</div>
      <button type="">Forward</button>
    </div>
  );
}
